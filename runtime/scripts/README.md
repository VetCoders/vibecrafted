# Runtime Scripts (Phase 2+)

Reserved for canonical runtime script entry points moved out of skill folders.
Current phase keeps shell helpers in `runtime/helpers/` and leaves this directory
as the contract surface for future migration.
