#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHONPATH="$ROOT/vibecrafted-core${PYTHONPATH:+:$PYTHONPATH}" \
  python3 -m vibecrafted_core.ship "$@"
