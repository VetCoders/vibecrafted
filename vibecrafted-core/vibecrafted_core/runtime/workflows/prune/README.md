# vc-prune Runtime

This package owns the default repository-health discovery brief used when the
operator runs `vibecrafted prune` or `vibecrafted prune <agent>` without
`--prompt` or `--file`.

The corresponding interactive doctrine remains in `skills/vc-prune/`.
