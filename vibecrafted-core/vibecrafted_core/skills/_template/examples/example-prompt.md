# {{SKILL_NAME}} — example trigger

## Trigger phrase

> TODO — paste a realistic operator message that should activate this skill.
> Example: "Run {{SKILL_NAME_NO_PREFIX}} on the payments module before we ship."

## Expected agent behavior

1. TODO — first concrete move the agent should make (e.g. re-read X, call
   `loctree context`, etc.).
2. TODO — second move.
3. TODO — terminal deliverable (file written, report emitted, commit landed).

## Acceptance evidence

What the operator should see in the final agent report:

- TODO — concrete artifact path or output snippet
- TODO — quality-gate result (e.g. `make test-skills` green)
- TODO — falsifier evidence proving the move actually happened

## Notes

- This is a **template** scaffolded {{CREATED_DATE}}. Replace every TODO before
  shipping. Add more example files alongside this one as the skill matures.
- Examples are the single most useful thing for future agents picking up this
  skill cold. Treat them as the contract surface, not an afterthought.
