schema = "vibecrafted.dispatch.v2"

[meta]
name = "invalid"
repo = "/tmp/vibecrafted-dispatch-fixture"

[[cuts]]
id = "c1"
agent = "codex"
workflow = "implement"
prompt = "Do it."

  [[cuts.verify]]
  run = "true"
  expect = { exit_code = 0 }
