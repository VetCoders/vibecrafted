schema = "vibecrafted.dispatch.v1"

[meta]
name = "hard-stop-release"
repo = "/tmp/vibecrafted-dispatch-fixture"

[[cuts]]
id = "c1"
agent = "codex"
workflow = "implement"
prompt = "Do it."

  [[cuts.verify]]
  run = "make release"
  expect = { exit_code = 0 }
