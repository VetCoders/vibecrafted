schema = "vibecrafted.dispatch.v1"

[meta]
name = "hard-stop-no-verify"
repo = "/tmp/vibecrafted-dispatch-fixture"

[[cuts]]
id = "c1"
agent = "codex"
workflow = "implement"
prompt = "Do it."

  [[cuts.verify]]
  run = "git commit --no-verify -m skip"
  expect = { exit_code = 0 }
