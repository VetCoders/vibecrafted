interface:
  display_name: "VibeCraft Delegate"
  short_description: "Native in-session subagent delegation"
  default_prompt: "Use $vc-delegate to delegate small bounded tasks to Claude subagents via the Task tool without Terminal spawning."
