interface:
  display_name: "VibeCraft Partner"
  short_description: "Executive debug + agent swarms"
  default_prompt: "Use $vc-partner to split this workflow into research tracks, run planner swarms, and resume the same sessions into implementation."

policy:
  allow_implicit_invocation: true
