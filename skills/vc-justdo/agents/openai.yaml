interface:
  display_name: "VibeCraft Ship"
  short_description: "Full Build-Converge-Ship pipeline"
  default_prompt: "Use the VibeCraft pipeline to run the complete process: init, workflow, followup, marbles convergence, dou audit, decorate, hydrate, and release."
