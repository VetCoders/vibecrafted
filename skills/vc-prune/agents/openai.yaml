interface:
  display_name: "VibeCraft Prune"
  short_description: "Prune repos to runtime or publish cone"
  default_prompt: "Use $vc-prune to classify runtime/build/publish surface, route non-trivial prune work through external VibeCraft agents, and plan a proof-driven cleanup."
