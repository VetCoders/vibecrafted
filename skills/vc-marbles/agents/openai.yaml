interface:
  display_name: "VC Marbles"
  short_description: "$vc-marbles is Truth-convergence executor. Use when implementation already exists but the codebase still lies."
  default_prompt: 'The overgenerated surfaces, drift between runtime paths, false certainty from one-shot agent output, or a product that "_works_" while remaining fragile.'
